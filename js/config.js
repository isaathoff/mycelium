// Site PIN, stored as a SHA-256 hash (not plain text) so it's not trivially
// visible in "view source" — see README.md "Changing the PIN" for how to
// generate a new hash and swap it in here.
const SITE_PIN_HASH = "d34a569ab7aaa54dacd715ae64953455d86b768846cd0085ef4e9e7471489b7b";
